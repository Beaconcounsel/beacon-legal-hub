In the collapsed "Book a Consultation" CTA above the footer, remove the extra WhatsApp phone-number link (the `<a href="tel:+250788559603">` line showing "WhatsApp: {WHATSAPP_DISPLAY}"), leaving only the main "Book a Consultation" button and the WhatsApp button CTA.

Then run the build and WhatsApp audit to verify no errors.